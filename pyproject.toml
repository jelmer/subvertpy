[build-system]
requires = ["setuptools", "requests"]
build-backend = "setuptools.build_meta"

[tool.ruff]
exclude = [
    "build",
    ".git",
    "build-pypy",
    ".tox",
    "subversion-*",
    "serf-*",
]
